import 'package:flutter/material.dart';
import 'src/app.dart';
import 'package:flutter/rendering.dart';

void main() {
  // debugPaintSizeEnabled = true;   // to see the positions/boundaries of layouts

  runApp(App());
}

